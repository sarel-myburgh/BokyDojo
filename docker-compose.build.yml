# =============================================================================
# Overlay for building from this checkout instead of pulling the published
# image. Only needed when working on the app itself.
#
#   podman-compose -f docker-compose.yml -f docker-compose.build.yml up -d --build
# =============================================================================
services:
  web:
    build: .
  worker:
    build: .
